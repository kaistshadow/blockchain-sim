import mmdb = require('maxmind');
export default class ConnectionType {
    readonly connectionType: string;
    ipAddress?: string;
    network?: string;
    constructor(response: mmdb.ConnectionTypeResponse);
}
